# ============================================================
# Editor & IDE
# ============================================================
.vscode/
.idea/
*.swp
*.swo
*~
.*.swp
.DS_Store

# ============================================================
# Language servers / linters
# ============================================================
.ccls-cache/
.cache/
.clangd/
compile_commands.json

# ============================================================
# C/C++ build artifacts (global fallback)
# ============================================================
*.o
*.d
*.a
*.so
*.dylib
*.out

# ============================================================
# Simulation & waveform
# ============================================================
*.log
*.vcd
*.fsdb
*.fst
*.wlf
*.wdb
*.jou
*.str
*.bak
*.mem
Makefile.html

# ============================================================
# Cscope & CTags
# ============================================================
tags
cscope.out
cscope.in.out
cscope.po.out

# ============================================================
# Python
# ============================================================
__pycache__/
*.pyc
*.pyo

# ============================================================
# Build directories — by project
# ============================================================
nemu/build/

abstract-machine/build/
am-kernels/**/build/

nanos-lite/build/
navy-apps/build/
navy-apps/fsimg/build/
navy-apps/fsimg/bin/
navy-apps/fsimg/share/

nvboard/build/

# NPC
npc/build/
npc/obj_dir/
npc/target/
npc/project/target/
npc/project/project/
npc/npc-trace.txt

# ysyxSoC
# 注意：必须写 build/* 而不是 build/。git 不会进入被排除的目录，
# 所以对 build/ 目录整体排除时，下面这条 ! 例外规则不会生效。
ysyxSoC/build/*
!ysyxSoC/build/ysyxSoCFull.v
ysyxSoC/target/
ysyxSoC/project/target/
ysyxSoC/rocket-chip/build/
ysyxSoC/rocket-chip/target/

# ============================================================
# docs —— 第三方知识库资料
# ------------------------------------------------------------
# 由 docs/fetch.sh 从官方地址抓取，不纳入版本控制。
# 不入库的理由是再分发权：RISC-V 规范(CC-BY)、GNU 手册(GFDL) 转发无碍，
# 但 OSTEP 与 xv6 讲义作者仅授权免费下载阅读，未授权第三方转发。
# 保留脚本与索引即可复现，无需替作者做分发决定。
#
# 只忽略 NN-xxx 形式的编号领域目录，docs/ysyx/（自己整理的讲义副本）
# 与 docs/README.md、docs/fetch.sh 照常跟踪。
# ============================================================
docs/[0-9][0-9]-*/

# ============================================================
# FPGA toolchains
# ============================================================
*.bit
*.dcp
*.edf
*.jed
*.mcs
*.prm
*.rbt
*.runs/
*.sim/
*.xpr
*.dSYM/
